import { Grid, GridItem } from '@chakra-ui/layout';
import React from 'react';

export const WeeklyColumn = ({ colNum }) => {
	return (
		<>
			{/* {timeline.map((time) => (
				<GridItem
					key={time}
					borderWidth='1px'
					colSpan={15}
					rowSpan={1}
					colStart={colNum}
					rowStart={0}
				>
					{time}
				</GridItem>
			))} */}
		</>
	);
};
